apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: orders-project-staging-catalog-service
spec:
  hosts:
  - "catalog-service.orders-project-staging.40.112.54.32.xip.io"
  gateways:
  - orders-project-staging-gateway
  http:
    - route:
      - destination:
          host: catalog-service.orders-project-staging.svc.cluster.local
